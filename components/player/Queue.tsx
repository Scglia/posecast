import { motion } from "framer-motion";
import { useEffect, useState } from "react";

const Queue = () => {
  return (
    <div
      style={{
        height: "300px",
        width: "100%",
        backgroundColor: "red",
      }}
    >
      Test
    </div>
  );
};

export default Queue;
